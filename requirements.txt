# BuildCheck Runtime Dependencies
# These are the minimum required package versions for build-check tools.
# Versions are based on Ubuntu 24.04 LTS or actual code requirements (whichever is higher).

# Required for package version verification (used by lib/package_verification.py)
packaging>=24.0

# Required for graph analysis and dependency tracking
# Used by: buildCheckDSM, buildCheckDependencyHell, buildCheckIncludeGraph,
#          buildCheckLibraryGraph, buildCheckOptimize, buildCheckRippleEffect
networkx>=2.8.8

# Required for statistical analysis (mean, stddev, percentiles)
# Used by: buildCheckDSM for coupling distribution analysis
numpy>=1.24.0

# Required for Git operations
# Used by: buildCheckRippleEffect
GitPython>=3.1.40

# Optional: For colored terminal output (gracefully falls back if missing)
# Used by: all scripts via lib/color_utils
colorama>=0.4.6

# Development tools
black>=24.0
